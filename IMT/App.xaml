<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:IMT"
             x:Class="IMT.App">
    <Application.Resources>
        <ResourceDictionary> 

            <!-- Цвета -->
            <Color x:Key="PrimaryColor">#4E73DF</Color>
            <Color x:Key="SecondaryColor">#A5B4FC</Color>
            <Color x:Key="AccentColor">#10B981</Color>
            <Color x:Key="BackgroundColor">#F9FAFB</Color>
            <Color x:Key="TextColor">#1F2937</Color>
            <Color x:Key="DisabledColor">#D1D5DB</Color>

            <!-- Шрифты -->
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource TextColor}, Dark={StaticResource BackgroundColor}}" />
                <Setter Property="FontFamily" Value="OpenSansSemibold"/>
            </Style>

            <Style TargetType="Entry">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{StaticResource TextColor}" />
                <Setter Property="BackgroundColor" Value="White" />
                <Setter Property="Margin" Value="0,4" />
                <Setter Property="HeightRequest" Value="40"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="30" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HeightRequest" Value="45" />
                <Setter Property="Margin" Value="0,6" />
            </Style>

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource BackgroundColor}" />
                <Setter Property="Padding" Value="20" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
